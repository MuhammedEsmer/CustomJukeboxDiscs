# 0.2.2

## Added

- `urlUploads.allowPrivateAddresses`, for operators whose audio host runs on the same machine or
  the same local network as the server. It stays off by default.

## Notes

Link writing accepts direct audio files only. Streaming services are deliberately not supported.
